# DISABLED — GitHub Pages cannot host Next.js apps with API routes.
# The app is deployed via Vercel (automatic on push to main).
# See .github/workflows/ci.yml for the active CI workflow.
name: Deploy Next.js to Pages (disabled)
on:
  push:
    branches: ["__disabled__"]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
